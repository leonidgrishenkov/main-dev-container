services:
  dev:
    build:
      dockerfile: ./Dockerfile
      context: .
    environment:
      - USER_ID=${USER_ID}
      - GROUP_ID=${GROUP_ID}
      - USERNAME=${USERNAME}
    container_name: devcr
    volumes:
      - ./src:/app
    command: ["tail", "-f", "/dev/null"]
    restart: no
